<?php
    //Komentar
        //Untuk menjelaskan baris kode atau mematikan kode tanpa menghapus

    //Standart Output
        //Untuk menampilkan sesuatu pada sisi client
            // 1. echo      => untuk memunculkan teks biasaya
            // 2. print     => untuk memunculkan texk biasa juga
            // 3. print_r
            // 4. var_dump

    //Sintaks PHP
        // Penulisan PHP bisa dmenggunakan:
            // 1. PHP di dalam HTML
            // 2. HTML di dalam PHP

    //Variabel
        // Menampung sebuah nilai
            // Penulisannya tinggal diberi simbol $ di depannya
            // Tidak boleh diawali angka, tapi boleh mengandung angka
            // Jika membutuhkan spasi maka menggunakan under-scors
    
    //Operator 
        // Aritmatika: +, -, *, /, dan %
        // Penugasan: =, +=, -=, /=, *=, dan %=.
        // Perbandingan: <, >, <=, >=, ==, dan !=
        // Logika: &&, ||, dan ! 
        // Identitas: ===, !==
?>

